global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'cargo-pulse-local'
    # Use 'host.docker.internal' to reach services running on the host machine (localhost)
    static_configs:
      - targets: ['host.docker.internal:8080']
    metrics_path: '/actuator/prometheus'