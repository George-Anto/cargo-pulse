spring:
  application:
    name: cargo-pulse

  config:
    import:
      - optional:file:./jwt-secret.properties

  profiles:
    active: ${CARGOPULSE_PROFILE:dev}

swagger:
  enabled: true
  path: /swagger-ui/index.html

jwt:
  expiration-ms: 3600000 # 1 hour